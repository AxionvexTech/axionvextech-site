export const metadata = { title: "Tasks — AxionvexTech Portal" };

export default function TasksPage() {
  return (
    <div className="space-y-4">
      <h1 className="text-xl font-semibold">Tasks</h1>
      <div className="rounded border border-slate-800 bg-slate-900/40 p-6 text-sm text-slate-400">
        Task assignment is scheduled for v1.1. For now, managers coordinate tasks outside the portal.
      </div>
    </div>
  );
}
